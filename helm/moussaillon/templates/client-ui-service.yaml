apiVersion: v1
kind: Service
metadata:
  name: {{ include "moussaillon.fullname" . }}-client-ui
  labels:
    {{- include "moussaillon.labels" . | nindent 4 }}
    app.kubernetes.io/component: client-ui
spec:
  type: {{ .Values.clientUi.service.type }}
  ports:
    - port: {{ .Values.clientUi.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: client-ui
    app.kubernetes.io/instance: {{ .Release.Name }}
